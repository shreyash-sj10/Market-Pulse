export default function TradeFeatureShell() {
  return (
    <div
      style={{
        background: "var(--v2-bg-card)",
        border: "1px solid var(--v2-border-subtle)",
        borderRadius: "0.75rem",
        color: "var(--v2-text-secondary)",
        padding: "1rem",
      }}
    >
      trade feature placeholder
    </div>
  );
}
