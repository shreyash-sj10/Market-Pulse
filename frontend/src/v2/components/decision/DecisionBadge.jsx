export default function DecisionBadge() {
  return (
    <div
      style={{
        background: "var(--v2-bg-card)",
        border: "1px solid var(--v2-border-subtle)",
        borderRadius: "0.5rem",
        color: "var(--v2-text-secondary)",
        fontSize: "0.875rem",
        padding: "0.75rem 1rem",
      }}
    >
      decision component placeholder
    </div>
  );
}
